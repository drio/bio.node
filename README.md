# bio.node - A node.js app to perform various nextgen analysis

  This is the code for a node.js app that allows me to run typical nextgen 
  analysis via a web interface (and a set of RESTful services).
